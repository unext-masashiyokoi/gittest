
<div class="row">
  <div class="col-md-8">
<p id="notice"><%= notice %></p>
<p>
  <h1 class="h3"><%= @blog.title %></h1>
</p>
<p><%= link_to image_tag(@blog.user.image, class:"img-circle", style: 'width:50px;' ), username_path(@blog.user.name) %><label class="label"><%= link_to @blog.user.name, username_path(@blog.user.name) %></label>
      <i class="fa fa-rss-square fa-lg fa-green"></i>
      <i class="fa fa-check-square fa-orange fa-lg"></i>
<i class="glyphicon glyphicon-time"></i>
<label class="label"><%= @blog.created_at.strftime('%Y/%m/%d %H:%M:%S') %></label>
</p>
<!--
<p>
  <i class="glyphicon glyphicon-tags"></i>
  <% for genre in @blog.genres %>
  <label class="label"><%= link_to genre.title, genre %></label>
  <% end %>
</p>

<p>
  <i class="glyphicon fa fa-save"></i>
  <% for journal in @blog.journals %>
  <label class="label"><%= link_to journal.title, journal %></label>
  <% end %>
</p>
-->
<p>
  <%= simple_format( @blog.content, {}, {:sanitize => false}) %>
</p>
<p><%= link_to image_tag(@blog.user.image, class:"img-circle", style: 'width:50px;' ), username_path(@blog.user.name) %><label class="label"><%= link_to @blog.user.name, username_path(@blog.user.name) %></label>
      <i class="fa fa-rss-square fa-lg fa-green"></i>
      <i class="fa fa-check-square fa-orange fa-lg"></i>

<i class="glyphicon glyphicon-time"></i>
<label class="label"><%= @blog.created_at.strftime('%Y/%m/%d %H:%M:%S') %></label>
</p>

<% if user_signed_in? && current_user.id == @blog.user.id %><%= link_to 'Edit', edit_blog_path(@blog) %> |<% end %>
<%= link_to 'Back', :back, class:"animsition-link" %>
</div>
  <div class="col-md-4">
<!-- <div id="calendarBlogs"></div> -->
</div>
</div>
